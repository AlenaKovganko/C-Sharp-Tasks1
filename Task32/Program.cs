// Задать массив из 8 элементов, заполненных нулями и единицами вывести их на экран
int [] A = {0, 0, 0, 0, 0, 0, 0, 0};

int index = 0;

 Console.WriteLine("Mассив из 0");
index = 0;
while(index < 8)
{
    Console.WriteLine(A[index]);
    index++;
}